# AGENTS.md — AIへの指示ファイル

> このファイルは、Codex(AI)が毎セッションの最初に読む「約束ごと」です。
> 短く保つこと(トークン節約のため)。詳細は各ドキュメントに分離する。

## プロジェクト概要

**スイカゲーム(オンラインランキング付き)**

フルーツを落として同じ種類を合体させるパズルゲーム。
GitHub Pagesで公開し、Supabaseでオンラインランキングを共有する。
ユーザーは朱音(あかね)さん。開発はAIが担当し、朱音さんは要件確認・動作確認・OK出しを行う。

## 最重要ルール(必ず守ること)

1. **セッション開始時に読むのは このファイル と `docs/PHASES.md` の「📍現在地」だけ**。
   REQUIREMENTS / DESIGN / SECURITY は該当フェーズの作業時のみ参照する(トークン節約)。
2. **作業後に必ず記録を残す** — `CHANGELOG.md` に追記し、`docs/PHASES.md` の現在地・タスク状態を更新する。
3. **要件は `REQUIREMENTS.md` が正** — 勝手に仕様を増やさない。
4. **セキュリティは `docs/SECURITY.md` に従う** — 特にランキング送信まわり。
5. **不明点は推測で進めず、朱音さんに聞く**。
6. **1タスク = 1まとまり** — フェーズ末に必ず朱音さんの動作確認を挟む。

## 技術方針

- バニラ HTML / CSS / JavaScript + **Matter.js(`src/lib/` に同梱、CDN非依存)**。ビルド不要。
- バックエンドは **Supabase**(無料枠)。スコア送信は Edge Function 経由のみ。
- 公開は **GitHub Pages**(`src/` 配下が公開対象。`supabase/` は公開サイトに含めない)。
- 上記以外の依存ライブラリは原則なし。必要なら理由を添えて提案する。

## やってはいけないこと

- 要件にない機能を勝手に追加する
- ニックネーム等を `innerHTML` で描画する(XSS。必ず `textContent`)
- Supabase の service role キーをクライアントコードやリポジトリに置く
- 記録ファイル(CHANGELOG / PHASES)の更新を忘れる

## ファイルの役割早見表

| ファイル | 役割 | 読むタイミング |
|---|---|---|
| `AGENTS.md` | このファイル | 毎セッション |
| `docs/PHASES.md` | フェーズ・タスク管理 | 毎セッション(現在地のみ) |
| `REQUIREMENTS.md` | 要件定義 | 仕様に迷ったとき |
| `docs/DESIGN.md` | デザイン方針 | 見た目を作るとき |
| `docs/SECURITY.md` | セキュリティ方針 | ランキング・公開作業のとき |
| `CHANGELOG.md` | 変更履歴 | 追記のみ(読み返し不要) |
| `src/` | ゲーム本体 | 作業対象のみ |
| `supabase/` | サーバー側定義 | F5以降 |
